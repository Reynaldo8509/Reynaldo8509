# Plan APPLY recomendado — sincronización segura

## Estado del DRY RUN

- Rama local inspeccionada: `publish/finalize-evidence-20260815-210501`.
- HEAD local: `c336974997adf3e7cbcaf9f9cbe4158bac5a874a`.
- Referencia local en caché `origin/main`: `e8af044464d09e4c817597c43afd382401857cf1`.
- El acceso remoto no pudo verificarse: `git fetch` y `git remote show origin`
  devolvieron `Repository not found`. Por ello la referencia `origin/main` puede
  estar desactualizada.
- Simulación aislada de merge contra esa referencia: sin conflictos (`RC 0`),
  abortada y eliminada sin modificar el árbol de trabajo principal.

Los snapshots creados antes de la simulación están en `GitBackup/snapshots/`:

- `soc-operations-lab.bundle.20260815-213439`
- `soc-operations-lab-tree.20260815-213439.tar.gz`

Sus SHA-256 se registran en [snapshot-20260815-213439.txt](snapshot-20260815-213439.txt).

## Clasificación

No hay conflictos textuales, pero el rango contiene 157 rutas y siete archivos
de automatización/reglas (`tools/*.py`, `tools/*.sh` y XML de reglas en
evidencia). Por tanto no debe resolverse automáticamente como si fuera sólo
contenido duplicado de documentación o imágenes.

## APPLY propuesto

1. Confirmar la identidad del repositorio remoto y que `git ls-remote origin
   refs/heads/main` devuelve una referencia actual. No cambiar la URL de
   `origin` sin confirmar el destino.
2. Crear ramas de recuperación, sin sobrescribir ninguna existente:

   ```bash
   git branch backup-local-20260815-213439 HEAD
   git branch backup-remote-20260815-213439 origin/main
   ```

3. Crear `sync/20260815-213439` desde el `origin/main` recién obtenido y
   fusionar la rama local mediante una revisión explícita.
4. Para archivos de contenido duplicado ya integrados en el perfil (README,
   documentación e imágenes), preferir la versión remota **sólo** después de
   comparar el inventario y mantener la versión local en `backup-local-*`.
5. Para `tools/` y reglas/XML, revisar semántica y pruebas Wazuh; no usar
   `--ours`, `--theirs` ni resolución masiva. Conservar la implementación local
   que cuente con evidencia de validación hasta confirmar la versión remota.
6. Ejecutar `git diff --check`, `git fsck --full`, comprobación de PII y la
   matriz de pruebas de cardinalidad antes de publicar.
7. Publicar `sync/20260815-213439` y abrir PR. No hacer push directo a `main`
   salvo que el remoto, la revisión de cambios y todos los checks estén verdes.

## Acciones no realizadas

Este DRY RUN no creó commits, no hizo merge en el árbol principal, no hizo push
y no modificó ramas remotas.
